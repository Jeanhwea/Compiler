{program bubble sort;}
var a:array[5] of integer {= (4, 5, 2, 7, 0)};
	i, j: integer;
procedure swap(var x:integer;var y:integer);
	var t: integer;
	function Fun(): integer;
		var va,A,r,d: integer;
		begin
			a[4] := -r*d;
			A := d
		end;
	begin
		t := x + y;
		x := y;
		y := t
	end;
procedure Proa(x,y:char);
	const CON = 99;
	function funIIN(x:integer;y:char): char;
		procedure funIIN();
			begin end;
		begin
			i := CON
		end;
	begin
		Proa(i,j);
	end;
begin { main }
	for i := 1 to 5 do
	begin
		for j := i to 5 do
		begin
			if a[i] > a[j] then
			begin
				swap(a[i], a[j])
			end;
		end;
	end;
	for i := 1 to 5 do
	begin
		write(a[i])
		{write(' ')}
	end
	{writeln()}
end.

