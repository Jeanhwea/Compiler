\section{State Machine}
\label{sec:sm}
\begin{tikzpicture}[
       >=stealth,
       node distance=6cm,
       on grid,
       auto
     ]
     \node[initial,initial text=,state] (START) {$\{START\}$};
	 \node[state] (INCOM) [below of=START] {$\{INCOM\}$};
     \node[state] (INIDE) [right of=START] {$\{INIDE\}$};
     \node[state] (INNUM) [above right of=START] {$\{INNUM\}$};
     \node[state] (INASS) [below right of=START] {$\{INASS\}$};
     \node[state,accepting] (DONE) [right of=INIDE] {$\{DONE\}$};

	 \path[->] (START) edge[loop above] node { white space } (START);
	 \path[->] (INCOM) edge[loop below] node { other } (INCOM);
	 \path[->] (INNUM) edge[loop above] node { digit} (INNUM);
	 \path[->] (INIDE) edge[loop above] node { letter } (INIDE);
	 \path[->] (START) edge[bend right] node{ \{ } (INCOM);
	 \path[->] (START) edge node{ digit } (INNUM);
	 \path[->] (START) edge node{ letter } (INIDE);
	 \path[->] (START) edge node{ : } (INASS);
	 \path[->] (START) edge[bend right] node{ other } (DONE);
	 \path[->] (INCOM) edge[bend right] node{ \} } (START);
	 \path[->] (INNUM) edge[bend left] node{ other } (DONE);
	 \path[->] (INIDE) edge node{ other } (DONE);
	 \path[->] (INASS) edge[bend right] node{ = } (DONE);
     
%     \path[->] (0) edge[loop above] node {0} (0);
%     \path[->] (0) edge[bend left] node {1} (1);
%     \path[->] (1) edge node {1} (2);
%     \path[->] (1) edge[bend left] node {0} (0);
%     \path[->] (2) edge[loop above] node {1} (2);
%     \path[->] (2) edge node {0} (3);
%     \path[->] (3) edge[loop above] node {0,1} (3);
%     
  \end{tikzpicture}
